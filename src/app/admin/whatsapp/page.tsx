"use client";

import { useState } from "react";
import { toast } from "sonner";

export default function AdminWhatsapp() {
    return (
        <div>
            <h2 className="text-xl font-bold">Regional WhatsApp groups</h2>
            <p className="mt-1 text-sm text-muted-foreground">This functionality is not currently integrated with the API yet.</p>
        </div>
    );
}
